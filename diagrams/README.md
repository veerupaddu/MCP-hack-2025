# 📐 Architecture Diagrams

Visual documentation for the MCP SDLC Agent system.

## 📁 Diagrams

| File | Description |
|------|-------------|
| `rag-finetune-system.svg` | Complete system architecture |
| `1-indexing-flow.svg` | RAG document indexing flow |
| `2-query-flow.svg` | RAG query and retrieval flow |
| `3-web-endpoint-flow.svg` | Web API endpoint flow |
| `4-container-lifecycle.svg` | Modal container lifecycle |
| `finetuning.svg` | Fine-tuning pipeline |

## 🏗️ System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    MCP SDLC Agent                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   MCP Server │  │    Agent     │  │  Dashboard   │      │
│  │   (Gradio)   │  │   (FastAPI)  │  │   (Flask)    │      │
│  │   :7860      │  │   :8001      │  │   :8000      │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                 │                 │               │
│         └────────────┬────┴─────────────────┘               │
│                      │                                      │
│         ┌────────────┴────────────┐                        │
│         ▼                         ▼                        │
│  ┌─────────────────┐    ┌─────────────────┐                │
│  │   Dual RAG API  │    │  Fine-tuned LLM │                │
│  │   (Modal)       │    │   (Modal/vLLM)  │                │
│  └────────┬────────┘    └─────────────────┘                │
│           │                                                 │
│    ┌──────┴──────┐                                         │
│    ▼             ▼                                         │
│ ┌──────────┐ ┌──────────┐                                  │
│ │existing_ │ │product_  │                                  │
│ │products  │ │design    │                                  │
│ │(PDFs)    │ │(DOCX)    │                                  │
│ └──────────┘ └──────────┘                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

1. **Indexing**: PDFs/DOCX → Embeddings → ChromaDB
2. **Query**: User question → Dual RAG → Context retrieval
3. **Generation**: Context + Fine-tuned LLM → Response
